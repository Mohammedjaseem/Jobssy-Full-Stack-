import NotFound from "../components/layout/NotFound";

export default function NotFoundPage() {
    return (
        <NotFound />
    )
    }
// in next.js 404.js is used to display 404 page it will automatically display when page is not found